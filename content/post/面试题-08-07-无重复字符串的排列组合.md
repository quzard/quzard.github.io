+++
title = "面试题 08.07. 无重复字符串的排列组合"
date = "2020-08-26T15:38:21+08:00"
toc = true
tags = ["LeetCode", "回溯", "深度优先搜索 DFS"]
categories = ["LeetCode", "Medium"]
description = ""
+++


## 难度（中等）

无重复字符串的排列组合。编写一种方法，计算某字符串的所有排列组合，字符串每个字符均不相同。

**示例1:**

```
 输入：S = "qwe"
 输出：["qwe", "qew", "wqe", "weq", "ewq", "eqw"]
```

**示例2:**

```
 输入：S = "ab"
 输出：["ab", "ba"]
```

**提示:**

1. 字符都是英文字母。
2. 字符串长度在[1, 9]之间。

<!--more-->

```
class Solution {
public:
    vector<string> permutation(string S) {
        vector<string> res;
       dfs(S, res, 0);
       return res;

    }
    void dfs(string S, vector<string>& res,int index){
       if(index == S.length()){
          res.push_back(S);
       } else{
          for(int i = index; i < S.length(); ++i){
             swap(S[index], S[i]);
             dfs(S, res, index + 1);
             swap(S[index], S[i]);
          }
       }
    }
};
```